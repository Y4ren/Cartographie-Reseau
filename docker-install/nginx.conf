server {
    listen       3001;
    listen  [::]:3001;
    server_name  nginx;

    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }
}